import { yesNo } from '../../../helpers/yes-no';
import { module, test } from 'qunit';

module('Unit | Helper | yes no');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = yesNo(42);
  assert.ok(result);
});
